import { createActionGroup, emptyProps, props } from '@ngrx/store';
import { Ratios } from '../models/ratios.model';


export const RatiosActions = createActionGroup({
    source: 'Ratios',
    events: {
    },
});